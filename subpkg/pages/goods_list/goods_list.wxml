<view>
    <view class="goods-list">
      <view wx:for="{{goodsList}}" wx:key="goods_id" wx:for-item="goods" bind:tap="gotoDetail" data-goods="{{goods}}">
        <my_goods goods="{{goods}}"></my_goods>
      </view>
    </view>
</view>